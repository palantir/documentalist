dependencies:
  override:
    - npm install yarn
    - $(npm bin)/yarn install
test: true
deployment:
  preview:
    branch: /.*/
  npm:
    tag: /release-.*/
    owner: palantir
    commands:
      - npm run build
      # Publish
      - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
      - npm publish
